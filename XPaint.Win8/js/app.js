var App = function (toolbox, canvas) {

    this.init = function () {

        canvas.html();

        //var picker = new Storage.Pickers.FileOpenPicker();
        //picker.fileTypeFilter.append(".jpg");
        //picker.pickSingleFileAsync()
        //.then(function (fs) {
        //    return fs.openAsync(Storage.FileAccessMode.read);
        //});

    }
};